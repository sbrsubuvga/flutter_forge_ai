/// Kept in sync with `pubspec.yaml`.
const String kCliVersion = '0.2.1';

/// Name of the companion package the CLI targets.
const String kPackageName = 'flutterforge_ai';

/// Version constraint suggested by `flutterforge init`.
const String kPackageVersionConstraint = '^0.3.0';
